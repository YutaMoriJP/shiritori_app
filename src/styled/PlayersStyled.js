import styled from "styled-components";

const PlayersStyled = styled.article`
  display: flex;
  justify-content: center;
  gap: 15px;
  flex-wrap: wrap;
  > * {
    flex: 1 1 auto;
  }
`;

export default PlayersStyled;
